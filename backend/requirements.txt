# Core
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic>=2.12,<3
pydantic-settings==2.5.0

# Firebase
firebase-admin==6.5.0

# Authentication (Firebase Auth handles this, but keeping for optional custom tokens)
python-jose[cryptography]==3.3.0
python-multipart==0.0.12

# HTTP Client (for external APIs)
httpx==0.27.0

# PDF Generation
reportlab==4.2.0
jinja2
weasyprint

# Testing
pytest==8.3.0
pytest-asyncio==0.24.0

# Development
python-dotenv==1.0.1

# Google APIs (Calendar, Maps, Places)
google-api-python-client==2.114.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0
googlemaps==4.10.0
